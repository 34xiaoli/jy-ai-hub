# CPAHelper「Excel→Word 表格同步」详细实现原理拆解（操作手册版）

> 来源：CPAHelper For Word《Excel-Word 表格数据同步》用户操作手册（语雀）
> 分析人：Buddy ｜ 目的：把底层机制拆透，让你的 Agent 复刻同样能力
> 配套代码：`excel_word_sync_agent.py`(v2，已跑通 `--demo`)

---

## 一、整体架构（四层模型）

```
┌─ 交互层 ──┐   当前表 / 全部表 / 数据源 / 选项  四个页签
├─ 绑定层 ──┤   Word 表 ↔ Excel文件 + 工作表 + 【名称区域】 的映射
├─ 锚点层 ──┤   Excel 名称区域(Named Range)，前缀 CPAH_（隐藏、可复用）
└─ 引擎层 ──┘   读名称区域 → 预处理(表头/排序/列映射) → 写回 Word 表
```

**最重要的发现**：绑定的"锚点"不是原始 A1 区域，而是 **Excel 名称区域(Named Range)**。这是整个机制稳健性的核心，也是"更换数据源"能成立的前提。

---

## 二、核心机制：名称区域作为锚点（全文最关键）

手册明确写了「Excel 名称区域（用户不可见）」一节：

- 建立绑定时，系统读取当前 Excel 选区作为来源。
- **若选区已有完全一致的名称区域 → 复用它**；否则**自动创建一个隐藏名称区域，前缀 `CPAH_`**。
- 复用已有名称区域**不会覆盖用户原来的名称**。

锚点存的是"哪个 Excel 文件 + 哪个工作表 + 哪个名称区域"。为什么用名称区域而不是 `A2:D5`？

| 用原始 A1 区域 | 用名称区域（CPAHelper 的做法） |
|---|---|
| 插入/删除行列后引用错位 | 名称区域跟随单元格移动，引用稳定 |
| 无法"换文件" | 只要新 Excel 有**同名名称区域**，即可整体切换数据源 |
| 绑定元数据很脆弱 | 绑定元数据只存一个名字，健壮 |

> **对 Agent 的启示**：把"绑定"建模为"Word 表 ↔ 名称区域名"。v2 代码用 `openpyxl` 的 `DefinedName` 建立 `CPAH_` 前缀名称区域，复用/创建逻辑与手册一致。

---

## 三、功能入口与四页签

窗口在「校验/同步」分组 → Excel同步，含四个页签：

| 页签 | 作用 |
|---|---|
| **当前表** | 处理光标所在 Word 表；或把 Excel 选区插入为 Word 表 |
| **全部表** | 查看本文档全部绑定，执行选中/全部同步 |
| **数据源** | 按 Excel 文件查看关联表，支持更换数据源 |
| **选项** | 表头同步、右键菜单、高级字段显示 |

---

## 四、绑定流程拆解

### 4.1 已有 Word 表 → 绑定 Excel 选区
1. Excel 中选好区域（**先保存到磁盘**，否则路径记录不稳）；
2. Word 光标放入目标表；
3. 打开 Excel同步 → 当前表 → **刷新选区**；
4. 点 **绑定当前表**；
5. 绑定成功**立即自动同步一次**；
6. 以后光标在表内点 **同步当前表** 即可。

### 4.2 没有 Word 表 → 插入 Excel 选区
1. Excel 选区域 → Word 光标放段落 → 刷新选区；
2. 标题显示"可插入 Excel 选区"时点 **插入Excel选区**；
3. 在当前段下方插入 Word 表，**自动套用"一键表格调整"格式**；
4. **自动建立同步绑定**（不是普通复制粘贴，插完即可同步）。

### 4.3 无 Excel 底表 → 导出全部表并绑定
在「全部表」点 **导出全部表并绑定**：读当前 Word 全部表 → 生成新 Excel 工作簿 → 每张 Word 表绑定到导出的对应区域。默认放 Word 同目录，重名自动加序号。

### 4.4 表头行数
- 区分"表头区/数据区"，绑定时自动判断，可改。
- **默认只同步数据区**，保留 Word 模板表头文字；需要表头也跟随 Excel 更新，则在选项勾 **同步表头内容**。

---

## 五、同步流程拆解

- **单表同步**：当前表页点同步 / 或加载右键菜单后表格右键"同步数据"。
- **批量同步**：全部表 → **同步全部表**。
- ⚠️ **同步全部表前必须先保存 Excel**：引擎读的是磁盘上已保存的文件，未保存的改动读不到（与 v1 文章提醒一致）。

---

## 六、高级功能

### 6.1 拆分表 + 列映射（长表拆短表）
适用：Excel 十几列在 Word 放不下，拆成多张短表（如长期股权投资）。

手册推荐步骤：
1. 先把原始 Word 表绑定到完整 Excel 区域；
2. 光标放入已绑定表 → 当前表页 **拆分表**；
3. 系统在当前表**下方复制一张同格式表**，绑定到**同一个 Excel 来源**；
4. 手动删第一张表不需要的列、删第二张表不需要的列；
5. 分别在「表格配置」检查/填写**列映射**（Word 列 → Excel 源区域列号）；
6. 例：10 列宽表拆两张 → 表1 `1,2,3,4,5`，表2 `1,6,7,8,9,10`；
7. 各点 **保存表格配置** 再同步。

**为什么必须设列映射**：Word 删除列后，系统只知道"当前表有几列"，不知道这些列对应 Excel 的哪几列。列映射规则：
- 以 Word 当前表列数为准；
- 填 Excel 源区域列号，**必须从左到右递增**；
- **不能重复**填同一 Excel 列；
- **不能超**名称区域实际列数；
- 改后必须点保存配置。

> v2 已用 `column_map: [0,3]` 在 Demo 表1 复刻此能力（只取科目+变动）。

### 6.2 同步前排序 + 「其他项置底」
- 勾选后，Excel 未排序内容同步到 Word 时自动排序；
- 选按第几列、升/降序；
- **「其他项置底」**：仅当明细项名称**精确等于"其他"**才置底不参与排序；"其他费用""其他项目"等**正常参与排序**（v2 的 `apply_sort` 严格按此实现）。

### 6.3 更换数据源（基于同名锚点）
- 数据源页选左侧路径 → **更换数据源** → 选新 Excel → 确认匹配 → 同步。
- 原理：新 Excel 含**同名 CPAH_ 名称区域**即可匹配。仅文件夹移动、内容/名称区域一致时通常可修复。
- v2 提供 `swap_data_source()` 实现同样逻辑。

---

## 七、已知坑 & 边界（Agent 实现时务必处理）

| 现象 | 原因 | 处理 |
|---|---|---|
| 同步后数字**前面有空格** | Excel 单元格设了千分符格式，对齐空格被带入 | 单元格格式改 `#,##0.00;-#,##0.00;"-";@`；Agent 侧对读取值 `strip()`（v2 已做） |
| 按钮灰色 | 无有效 Excel 选区 / 光标不在表内 / 未绑定 / 正在同步 | 先刷新选区/绑定信息；Agent 侧做参数校验（v2 列映射校验） |
| 同步全部表读到旧数据 | Excel 未保存 | 同步前强制保存（Agent 可监控 mtime） |
| 拆表后同步多余列 | 列映射未保存/与当前列数不一致 | 保存配置限制 Excel 列（v2 `validate_column_map`） |
| 解除绑定 | 只清 Word 侧绑定信息 | 不删 Excel 文件、不删名称区域（可复用） |

---

## 八、让 Agent 复刻：功能 ↔ 实现映射表

| 手册功能 | Agent 实现方式 |
|---|---|
| 名称区域锚点 `CPAH_` | `openpyxl.DefinedName` 创建/复用；绑定存名称而非 A1 |
| 绑定当前表 | `bind()` 建名 + 写一条 bindings.json 记录 |
| 插入Excel选区并绑定 | 在 Word 插表(python-docx `add_table`) + 自动 `bind()` |
| 导出全部表并绑定 | 遍历 `doc.tables` → 各生成 Excel 表 + 各 `bind()` |
| 表头行数 | 绑定里 `skip_header_rows`；选项 `同步表头内容` 控制是否 skip |
| 同步当前表/全部表 | `sync_binding()` / `sync_all()` |
| 拆分表 | Word 复制同格式表(python-docx) + 各自 `column_map` |
| 列映射 | `apply_column_map()` + 校验（递增/不重复/不超界） |
| 同步前排序/其他置底 | `apply_sort()`，精确匹配"其他" |
| 更换数据源 | `swap_data_source()` 按同名名称区域匹配 |
| 右键菜单/选项 | Agent 不需要 GUI；用对话命令或自动化触发等价操作 |

---

## 九、技术选型（给俊仪的生产建议）

| 方案 | 技术 | 适合 |
|---|---|---|
| **原型（已交付 v2）** | `openpyxl` 读 + `python-docx` 写 | 快速验证、跨平台；写回会丢多 run 格式、合并表头易出问题 |
| **生产级（Windows）** | `win32com` 自动化 Word | **100% 保真**（合并表头/样式/内容控件）；逻辑复用，只换"写回"层 |
| **插件级（与原文一致）** | Office JS API / VSTO | 嵌 Word 内、可右键菜单；开发量大、要适配 WPS |

**落地路线**：用 v2 在你们项目组跑通"绑定+同步+排序+拆分列映射"主流程 → 正式报告换 `win32com` 写回层 → 挂 WorkBuddy 自动化（Excel 变更/定时触发）。

---

## 十、交付文件

- `excel_word_sync_agent.py` —— v2 同步引擎（名称区域锚点/表头/排序/列映射/数据源更换），`--demo` 自验证通过
- `excel_word_sync_demo/` —— 样例 `demo_TB.xlsx`(含 `CPAH_费用明细` 名称区域) / `demo_报告.docx`(完整表+拆分表) / `demo_bindings.json`

下一步可选：封装为 WorkBuddy 自动化（监听 Excel 变更自动同步），或补 `win32com` 生产版写回层。
