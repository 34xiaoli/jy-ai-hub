# CPAHelper「Excel→Word 表格同步」原理分析 & 让你的 Agent 做同样的事

> 来源文章：*改完 Excel ，Word 报告自动同步，别再手动贴表了*（公众号「茶瓜子的休闲馆」，2026-07-20）
> 分析人：Buddy ｜ 适用对象：俊仪（事务所四大业务线 AI 转型）

---

## 一、文章在解决什么问题

审计报告 / 招股书 / 专项说明里，**数据在 Excel（TB、附注底表、测算表），呈现却在 Word（报告、反馈回复）**。客户改数、调分录、改披露口径后，Word 里的几十上百张表都要跟着改——纯靠人工复制粘贴，是事务所最痛的重复劳动之一。

CPAHelper For Word 的方案一句话：**先绑定，再同步**。把 Word 每张表和 Excel 某块区域建立链接，之后 Excel 改完、Word 点一下「同步」即可刷新。

---

## 二、技术实现原理拆解

### 2.1 绑定（Bind）—— 核心是一份「表→数据源」的映射元数据

绑定就是告诉工具：**Word 的第 N 张表，对应 Excel 的 哪个文件 / 哪个工作表 / 哪个区域**。两种场景：

| 场景 | 操作 | 产物 |
|---|---|---|
| A. 有 Word 模板、没 Excel 底表 | Word 里点「导出全部表并绑定」 | 自动导出所有表到 Excel，并建立一一对应；Excel 即底表模板 |
| B. 两边都有（最常见） | 选 Excel 区域 + 选 Word 目标表 → 「绑定当前表」 | Word 记住来源文件/表/区域 |

> **关键点**：绑定关系必须"持久化"在文档或配置里，否则关掉重开就丢失。CPAHelper 大概率用以下之一存放绑定元数据（按可能性排序）：
> 1. **Word 表格的「标题/替换文字(Alt Text)」** 里塞一段 JSON，如 `{"excel":"TB.xlsx","sheet":"附注","range":"A2:F20"}`——无需额外文件，随文档走；
> 2. **Word 自定义 XML 部件（customXmlParts）**——更干净，但插件才能读写；
> 3. **配套 config 文件**——最简单，适合外部 Agent 方案。

### 2.2 同步（Sync）—— 读 Excel → 写 Word 表格

1. **必须先保存 Excel**（同步读的是已落盘的 .xlsx，不是内存态）；
2. 读取绑定区域 → 二维数组；
3. 按映射把值写回 Word 表格对应单元格；
4. 行数变化时要**插入/清空行**，列数变化时要处理（见 2.4）。

### 2.3 排序 & 「其他项置底」

披露口径常要求"金额大的排前面、其他放最后"，但底稿按科目编码排。所以同步前做一次**预处理排序**：
- 配置排序列 + 方向（升/降）；
- `其他项置底`：即使"其他"金额最大，也强行沉到末尾（按关键列值命中"其他"则隔离到尾部）。

### 2.4 列映射（长表拆分）

Excel 一张十几列的表在 Word 放不下，要拆成两张短表。列映射就是**给每张 Word 表指定它取 Excel 的哪几列**（如 表1→列[0,1,2,3]，表2→列[4,5,6,7]）。这样拆过一次后，后续更新仍能正确回填，不用手动再拆。

### 2.5 技术栈推断

CPAHelper 是**嵌在 Word 里的插件**（要同时适配 Office 与 WPS），最可能是：
- **Office Web Add-in**（Office JavaScript API，跨 Office/WPS 网页版）或
- **VSTO / WPS JS 宏**（原生 COM，格式保真度最高）。

难点在于：合并表头、特殊合计行、插入行时的版式、内容控件、右键菜单，在不同 Office/WPS 环境表现不一致——这也是作者说"还有 Bug"的根因。

---

## 三、让你的 Agent 实现同样操作：架构蓝图

你的 Agent 不必重写成 Office 插件。更轻、更契合你（React/TS + Python）技术栈的做法是：**Agent 作为"外部同步引擎的调度者"**。

```
┌──────────────┐   绑定配置(JSON)   ┌──────────────────────┐
│  你的 Agent   │ ─────────────────> │  bindings.json        │
│ (对话/自动化) │                    │ Word表 <-> Excel区域  │
└──────┬───────┘                    └──────────┬───────────┘
       │ 触发 sync                             │ 读取
       ▼                                       ▼
┌──────────────────────┐              ┌──────────────────┐
│  sync 引擎 (Python)   │ ───────────> │ Excel(.xlsx)      │
│  openpyxl + docx/com  │              └──────────────────┘
└──────────┬───────────┘
           │ 写回
           ▼
      ┌──────────┐
      │ Word文档 │
      └──────────┘
```

### 3.1 把"绑定"变成 Agent 的配置（bindings.json）

Agent 不需要住在 Word 内部，绑定关系用一份 JSON 管理（示例见下方）。每条绑定包含：文档路径、表序号、Excel 路径、工作表、区域、排序规则、列映射。

```json
{
  "bindings": [
    {
      "docx": "审计报告.docx",
      "table_index": 3,
      "excel": "TB.xlsx",
      "sheet": "附注底表",
      "range": "A2:F20",
      "skip_excel_header_rows": 1,
      "sort": { "column": 4, "direction": "desc", "other_bottom": true, "other_key": "其他" },
      "column_map": [0,1,2,3,4,5],
      "backup": true
    }
  ]
}
```

### 3.2 同步引擎（Agent 真正跑的东西）

引擎做四件事：**读 Excel 区域 → 排序/其他置底 → 列映射 → 写回 Word 表**。核心代码已附在 `excel_word_sync_agent.py`，含自验证 Demo（`python excel_word_sync_agent.py --demo`）。

### 3.3 触发方式（让 Agent"自动"干）

| 触发 | 实现 | 适合 |
|---|---|---|
| 对话命令 | 你发「同步审计报告」→ Agent 跑引擎 | 临时、按需 |
| 文件监听 | 监听 TB.xlsx 修改时间，变了就同步 | 持续协作 |
| 定时/自动化 | 每天下班前自动刷新一次 | 固定节奏 |

> 你用的是 WorkBuddy，可以直接把它做成**自动化任务**（监听 Excel 变更或定时触发），不必每次手动喊。

### 3.4 原型级 vs 生产级 技术选型

| 方案 | 库/技术 | 优点 | 局限 |
|---|---|---|---|
| **原型（已附代码）** | `openpyxl` + `python-docx` | 跨平台、纯 Python、易读 | 写回会丢单元格内多 run 格式、合并单元格/复杂表头易出问题 |
| **生产级（Windows）** | `win32com` 自动化 Word | **100% 保真**，合并表头、样式、内容控件全保留 | 仅 Windows、需装 Office |
| **插件级（和原文一致）** | Office JS API / VSTO | 嵌在 Word 内、可右键菜单 | 开发量大、要适配 WPS |

**建议**：先用附带的 `python-docx` 原型在你们项目组跑通"绑定+同步+排序"主流程；对版式要求高的正式报告，改用 `win32com` 版本（逻辑完全复用，只换"写回"那一层）。

---

## 四、落地建议（给你的事务所场景）

1. **先标准化底表**：让项目组 TB/附注底表有固定 sheet 名和区域，绑定配置一次写好、长期复用。
2. **表头用 `skip_excel_header_rows` 跳过**：Word 表已带表头，Excel 区域包含表头时设 `skip_excel_header_rows: 1`，避免重复。
3. **披露口径用排序配置固化**：把"金额降序 + 其他置底"写进绑定，不再手工排。
4. **长表拆分用列映射**：Excel 宽表 → 多张 Word 短表，各自列映射，更新零手工。
5. **接自动化**：把引擎挂成 WorkBuddy 自动化，Excel 一保存就触发同步，真正"改完 Excel，Word 自动跟上"。

---

## 五、附：已交付文件

- `excel_word_sync_agent.py` —— 可运行的最小同步引擎（含 `--demo` 自验证）
- `excel_word_sync_demo/` —— Demo 生成的样例 TB.xlsx / 报告.docx / 绑定配置

下一步可选：我可以把这份引擎封装成你的专属 **WorkBuddy 自动化**（监听 Excel 变更自动同步），或补一个 `win32com` 生产版写回层。
